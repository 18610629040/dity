<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dity.common.aop.dao.LogDaoMapper">

    <insert id="insertLog" parameterType="map">
		insert into T_DITY_LOG_RECORD(
			ID,
			USER_ID,
			URL,
			METHOD_NAME,
			CONTENT,
			TIMESTR,
			CLIENT_IP) 
		values(#{ID},#{USER_ID},#{URL},#{METHOD_NAME},#{CONTENT},#{TIMESTR},#{CLIENT_IP})
	</insert>
    
	
</mapper>